# API / UI
APP_HOST=0.0.0.0
APP_PORT=8008
MLFLOW_TRACKING_URI=http://127.0.0.1:5001
MLFLOW_EXPERIMENT=void-train-manager

# Unified launcher/task catalog
TRAINING_CATALOG_PATH=./backend/config/training_catalog.yaml

# Catalog DB (required: YAML is stored in PostgreSQL revisions)
# Option A) full DSN
# CATALOG_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/void_train_manager
#
# Option B) discrete connection fields
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=void_train_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SSLMODE=disable

# Optional per-task runner overrides
# CLASSIFICATION_SCRIPT_PATH=/abs/path/to/train_classification.py
# SEGMENTATION_SCRIPT_PATH=/abs/path/to/train_segmentation.py

# FTP client defaults
FTP_DEFAULT_HOST=127.0.0.1
FTP_DEFAULT_PORT=2121
FTP_DEFAULT_USERNAME=mlops
FTP_DEFAULT_PASSWORD=mlops123!
# FTP_MODEL_CACHE_ROOT=~/.cache/torch/hub/void-train-manager/ftp-model-registry
